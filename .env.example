# PartnerLens — environment variables
# Copy this to .env and fill in your key — never commit .env

# Required — Claude API for intent detection
VITE_CLAUDE_API_KEY=sk-ant-your-key-here

# Transcription: Web Speech API is used (free, browser-native, no key needed)
# Works in Chrome on simulator and on real glasses
